import React, { FC } from 'react';

import { Container, ExpandIcon, Title } from './styles';

const GuildName: FC = () => {
  return (
    <Container>
      <Title>Rohit | 0</Title>

      <ExpandIcon />
    </Container>
  );
};

export default GuildName;
